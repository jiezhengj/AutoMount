# 项目核心开发准则 (Project Golden Rules)

## 1. 严禁在代码中硬编码配置数据

- **绝对禁止**在源代码中直接硬编码任何应当由配置文件承载的业务数据（包括但不限于特定的 IP 地址、MAC 地址、主机名、局域网域名、共享路径、用户名等）。
- **绝对禁止**在交互输入或回退逻辑中，将具体的个人/私有目标作为默认回车值。提示信息仅允许使用通用伪代码或语法示例（例如 `smb://server.local/share`），不能存在针对特定环境的私货。
- 所有写入配置文件的信息，必须严格来自**运行期的真实动态探测**或**用户在交互向导中的显式输入**。

## 2. 严禁在测试时代替程序伪造配置

- 在开发、调试与验证阶段，**绝对禁止** Agent 或开发者直接手工编辑、伪造或预写业务配置文件（如 `auto_mount.plist`）。
- 所有测试和正式配置，**必须且只能通过程序自身的功能**（如 `./auto_mount --init` 或内置初始化逻辑）在真实环境中运行生成。
- 如果程序自身无法自动化完成探测或推导，必须在代码层面实现该能力，严禁通过“人工代写配置”来假装程序具备该功能。

## 3. 公开技术文档与 README 写作规范

- **彻底杜绝对话残留与语境黑话**：严禁将 AI 与用户的讨论过程、沟通语境中的临时代号（如“陷阱 1/3/4”、“策略 B”、“闭环”、“痛点”等）写入公开技术文档与 README 中。
- **面向第三方的独立性与客观性**：公开文档的读者从未参与讨论。所有机制必须站在全新第三方的视角，使用标准、通用、客观的计算机网络与操作系统工程术语表述（例如：“断网失效挂载的超时强制清理”、“Tailscale 握手就绪延迟应对机制”、“Spotlight 索引防护”）。
- **严谨专业的开源文档结构**：文档结构必须清晰专业（产品定位 ➔ 核心特性 ➔ 快速上手 ➔ 配置规范 ➔ 日常维护 ➔ 技术原理 ➔ 常见问题），杜绝空洞大词与情绪化表达。
